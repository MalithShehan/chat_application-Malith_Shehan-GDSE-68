create database chatApplication;

use chatApplication;

create table User(
                     name varchar(15),
                     email varchar(35) primary key ,
                     password varchar(155)
);